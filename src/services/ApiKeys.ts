
// WARNING: These are just placeholder references - real API keys should NEVER be stored in frontend code
// In a real application, these should be stored securely in environment variables on the server
// or in Supabase Edge Functions secrets

export const API_KEYS = {
  // This is just a reference - the actual key should be stored in Supabase Edge Function secrets
  GEMINI_API: "geminei-api-key", 
  // This is just a reference - the actual key should be stored in Supabase Edge Function secrets
  OPENAI_API: "openai-api-key",
};
